@model IEnumerable<ATCScheduler.Models.ShiftATController>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ShfitId)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.ATController)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ShfitId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ATController.UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ShiftATControllerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ShiftATControllerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ShiftATControllerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
